# sort()- the numpy ndarray object has a function which is called sort(). and this will sort a specified array.
"""Sorting the 1-D array"""
import numpy as np 
pb=np.array([3,2,0,1])
# by default shorting asendingorder pahle chhota fir bara.
print(np.sort(pb)) #This method is like as copy().

# sort the array alphbetically first a then b then c
import numpy as np
pb=np.array(['banana','cherry','apple'])
print(np.sort(pb))

# sort the boolean array:-
import numpy as np
pb=np.array([True, False, True])
print(np.sort(pb))

"""Sorting the 2-D & 3-D array"""
import numpy as np
pb=np.array([[3,2,4],[5,0,1]])
print(np.sort(pb))


